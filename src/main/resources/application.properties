spring.application.name=deep-search
server.port=${SERVER_PORT:8080}

# ── PostgreSQL Database ──
# These values can be overridden by environment variables:
# SPRING_DATASOURCE_URL, SPRING_DATASOURCE_USERNAME, SPRING_DATASOURCE_PASSWORD
# IMPORTANT: JDBC URL must be in format: jdbc:postgresql://host:port/database
# Do NOT include username:password in the URL - use separate username/password properties
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d69deepr0fns7381p7p0-a:5432/deep_search_db}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:deep_search_db_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:SUyMu0knT1rgkp5jkZxDNLDohsYwEZoJ}

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
